import Section from "./section";

const Portfolio = () => {
  return (
    <div id="portfolio">
      <Section>
        <div>
          <h2>Portfolio section</h2>
          <p>Take a look at some examples of what I can do:</p>
          [EXAMPLES]
        </div>
      </Section>
    </div>
  );
};

export default Portfolio;
